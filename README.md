# Collider Drawer in Unity Editor Mode
A repository for imitating the Unity's drawings for colliders. A basic usage of the collider visualization could be the case that, if a collider is added in run-time, the size of collider can be visualized without adding the collider in editor mode.

---IN DEVELOPMENT---
